// The "lib" dirs is used as symlink in the ext project
// Without this index file we may end up with duplicate indentifiers
export { SummaryService } from './summary.service';
export { GeminiService } from './gemini.service';
